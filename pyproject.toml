[project]
name = "alpr-pro"
version = "1.0.0"
description = "Production-ready Automatic License Plate Recognition system with YOLO, OpenCV, dual OCR, FastAPI, Streamlit, and Docker."
readme = "README.md"
requires-python = ">=3.10,<3.13"

[tool.pytest.ini_options]
pythonpath = ["."]
